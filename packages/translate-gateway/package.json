{
  "name": "@web-services/translate-gateway",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@web-services/shared": "workspace:*",
    "hono": "^4.12.16",
    "zod": "^4.4.3"
  }
}
