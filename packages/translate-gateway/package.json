{
  "name": "@web-services/translate-gateway",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "hono": "^4.12.16",
    "@hono/zod-validator": "^0.8.0",
    "zod": "^4.4.3"
  }
}
