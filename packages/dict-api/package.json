{
  "name": "@web-services/dict-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "db:generate": "npx tsx scripts/import-ecdict.ts",
    "db:schema": "wrangler d1 execute ecdict-db --file=scripts/schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.9.0"
  }
}
